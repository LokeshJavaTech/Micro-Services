server:
  port: 8071

spring:
  application:
    name: configserver_ms

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          #search-locations: "classpath:/config"
          search-locations: "file:///D://workspace//Micro-Services//file-location-for-config-files"

